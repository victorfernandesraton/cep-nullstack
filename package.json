{
  "name": "cep-verify",
  "version": "0.0.1",
  "description": "",
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nullstack": "~0.15.0",
    "tailwindcss": "^3.1.8"
  },
  "scripts": {
    "start": "npx nullstack start",
    "build": "npm run build-tailwind && npx nullstack build --mode=ssg --output=static",
    "tw": "npx tailwindcss -i ./src/tailwind.css -o ./src/styles.css --watch",
    "tw-build": "npx tailwindcss -i ./src/tailwind.css -o ./src/styles.css"
  },
  "dependencies": {
    "cep-any": "3.1.1"
  }
}
